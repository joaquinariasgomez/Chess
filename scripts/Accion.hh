#pragma once
#include "mapa/Mapa.hh"

class Accion {
    public:
        /*Accion(std::string movimiento);
        int move(Mapa& t) const;
    private:
        int celdaX;
        int celdaY;
        char direccion;*/
};